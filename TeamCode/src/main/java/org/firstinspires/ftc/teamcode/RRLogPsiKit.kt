package org.firstinspires.ftc.teamcode

import android.R.attr.value
import com.acmerobotics.roadrunner.ftc.FlightRecorder
import org.psilynx.psikit.LogTable
import org.psilynx.psikit.io.LogDataReceiver
/*
class RRLogPsiKit: LogDataReceiver {
    override fun putTable(table: LogTable) {
        table.getAll(false).forEach { (key, data) ->
            //FlightRecorder.write(key, data.value)
        }
    }
}

 */